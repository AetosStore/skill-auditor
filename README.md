# skill-auditor
Audits every installed Claude Code skill — personal, project, and plugin — for quality issues, trigger overlaps, and risky script capabilities (credential access, pipe-to-shell, prompt injection). Produces a severity-rated Markdown report with a suggested fix per finding. Read-only by design; fixes applied only with explicit approval.
